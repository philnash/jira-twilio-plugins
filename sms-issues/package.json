{
  "name": "twilio-jira-plugin",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "devDependencies": {
    "twilio-run": "^3.5.2"
  },
  "engines": {
    "node": "10"
  },
  "dependencies": {
    "atlassian-jwt": "^1.0.3",
    "got": "^11.8.5"
  }
}
